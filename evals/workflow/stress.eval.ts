// Upstream compatibility marker. Native coverage exercises 100 sequential turns
// and 50 concurrent sessions in internal/workflow/store_test.go.
export default { description: "Durable sequential and concurrent workflow turns." };
